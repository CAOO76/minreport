# .env.example
# Copia este archivo a .env para desarrollo local y ajusta los puertos si es necesario.
# Este archivo es una plantilla y está bajo control de versiones.
# El archivo .env NO debe estar bajo control de versiones.

# --- Puertos para Servicios de Backend ---
ACCOUNT_SERVICE_PORT=8081
REGISTRATION_SERVICE_PORT=8082
REVIEW_SERVICE_PORT=8083

# --- Emuladores de Firebase (Backend) ---
# Los servicios de Backend (Node.js Admin SDK) leerán estas variables.
HOSTING_EMULATOR_PORT=5010

# --- Emuladores de Firebase (Backend) ---
# Los servicios de Backend (Node.js Admin SDK) leerán estas variables.
FIRESTORE_EMULATOR_HOST=localhost:8085
FIREBASE_AUTH_EMULATOR_HOST=localhost:9190

# --- Emuladores de Firebase (Frontend) ---
# Las aplicaciones Frontend (Vite) leerán estas variables con prefijo VITE_.
VITE_FIRESTORE_EMULATOR_HOST=localhost
VITE_FIRESTORE_EMULATOR_PORT=8085
VITE_AUTH_EMULATOR_PORT=9190

# --- API Keys ---
# API Key de Resend (para el servicio de registro de solicitudes)
RESEND_API_KEY=re_YOUR_RESEND_API_KEY