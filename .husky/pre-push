#!/bin/sh
# .husky/pre-push

pnpm -r build && pnpm -r test
RESULT=$?
if [ $RESULT -ne 0 ]; then
  echo "\n❌ Push cancelado: build o tests fallaron. Corrige los errores antes de subir."
  exit 1
fi
exit 0
