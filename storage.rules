rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Permitir lectura y escritura para desarrollo local si el usuario está autenticado.
      allow read, write: if request.auth != null;
    }
  }
}
