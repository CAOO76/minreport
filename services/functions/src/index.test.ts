import { describe, it, expect } from 'vitest';

// Import other test files
import './clientPluginManagement.test';
import './pluginApi.test';
import './tokens.test';

describe('Firebase Functions', () => {
  it('should pass a basic test', () => {
    expect(true).toBe(true);
  });
});
